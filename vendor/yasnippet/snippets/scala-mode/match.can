# Author: Jonas Bonèr <jonas@jonasboner.com>
# name: can match { case Full(res) => .. }
# key: match
# --
${1:option} match {
  case Full(res) => $0

  case Empty => 

  case Failure(msg, _, _) => 

}